# 🔐 Настройка GitHub Secrets для автоматического деплоя

## Почему workflow не работает?

Ваш workflow файл настроен правильно, но **GitHub не знает как подключиться к вашему VPS**, потому что секреты (credentials) не настроены. Давайте это исправим!

---

## 📋 Что нужно сделать

Вам нужно добавить **5 секретов** в настройки GitHub репозитория:

| Секрет | Описание | Пример |
|--------|----------|--------|
| `VPS_HOST` | IP адрес или домен вашего VPS | `123.45.67.89` |
| `VPS_USERNAME` | SSH пользователь на VPS | `root` или `ubuntu` |
| `VPS_SSH_KEY` | Приватный SSH ключ | Весь текст включая `-----BEGIN...-----` |
| `VPS_PORT` | SSH порт | `22` |
| `VPS_PROJECT_PATH` | Путь к проекту на VPS | `/var/www/web-site` |

---

## 🚀 Шаг 1: Подготовка VPS (если еще не сделано)

### 1.1 Подключитесь к VPS

```bash
ssh ваш_пользователь@ваш_vps_ip
```

### 1.2 Создайте SSH ключ для GitHub Actions

```bash
# Создайте ключ БЕЗ пароля (это важно!)
ssh-keygen -t rsa -b 4096 -C "github-actions" -f ~/.ssh/github_actions_key -N ""

# Добавьте публичный ключ в authorized_keys
cat ~/.ssh/github_actions_key.pub >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/github_actions_key
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh
```

### 1.3 Скопируйте приватный ключ

```bash
# Выведите приватный ключ на экран
cat ~/.ssh/github_actions_key
```

**Скопируйте ВЕСЬ текст**, включая строки:
```
-----BEGIN OPENSSH PRIVATE KEY-----
... (много строк) ...
-----END OPENSSH PRIVATE KEY-----
```

### 1.4 Склонируйте репозиторий на VPS (если еще не сделано)

```bash
# Создайте директорию
sudo mkdir -p /var/www/web-site
sudo chown -R $USER:$USER /var/www/web-site

# Клонируйте репозиторий
cd /var/www/web-site
git clone https://github.com/slaffka85/web-site.git .

# Проверьте
ls -la
```

---

## 🔐 Шаг 2: Добавление секретов в GitHub

### 2.1 Откройте настройки репозитория

1. Перейдите на https://github.com/slaffka85/web-site
2. Нажмите **⚙️ Settings** (справа вверху)
3. В левом меню выберите **Secrets and variables** → **Actions**
4. Нажмите зеленую кнопку **New repository secret**

### 2.2 Добавьте каждый секрет

#### Секрет 1: VPS_HOST

- **Name:** `VPS_HOST`
- **Value:** IP адрес вашего VPS (например: `123.45.67.89`)
- Нажмите **Add secret**

#### Секрет 2: VPS_USERNAME

- **Name:** `VPS_USERNAME`  
- **Value:** Ваш SSH пользователь (например: `root` или `ubuntu`)
- Нажмите **Add secret**

#### Секрет 3: VPS_SSH_KEY

- **Name:** `VPS_SSH_KEY`
- **Value:** Вставьте **ВЕСЬ** приватный ключ из шага 1.3
  ```
  -----BEGIN OPENSSH PRIVATE KEY-----
  b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAABlwAAAAdzc2gtcn
  ... (все строки) ...
  -----END OPENSSH PRIVATE KEY-----
  ```
- Нажмите **Add secret**

> **⚠️ ВАЖНО:** Скопируйте ключ полностью, без пробелов в начале/конце!

#### Секрет 4: VPS_PORT

- **Name:** `VPS_PORT`
- **Value:** `22` (обычно это стандартный SSH порт)
- Нажмите **Add secret**

#### Секрет 5: VPS_PROJECT_PATH

- **Name:** `VPS_PROJECT_PATH`
- **Value:** `/var/www/web-site` (путь где склонирован репозиторий)
- Нажмите **Add secret**

### 2.3 Проверьте секреты

После добавления всех секретов вы должны увидеть:

```
VPS_HOST
VPS_USERNAME
VPS_SSH_KEY
VPS_PORT
VPS_PROJECT_PATH
```

**Значения секретов скрыты** - это нормально и безопасно! ✅

---

## ✅ Шаг 3: Тестирование

### 3.1 Триггер workflow

Сделайте любое изменение и запуште:

```bash
# На вашем локальном компьютере
echo "<!-- Test deployment -->" >> README.md
git add .
git commit -m "Test auto-deploy"
git push
```

### 3.2 Проверьте GitHub Actions

1. Перейдите на https://github.com/slaffka85/web-site
2. Откройте вкладку **⚡ Actions** (вверху)
3. Вы увидите workflow **"Deploy to VPS"** в процессе выполнения

**Если всё настроено правильно:**
- ✅ Зеленая галочка через 30-60 секунд
- ✅ В логах увидите "Deployment completed successfully!"

**Если что-то не так:**
- ❌ Красный крестик
- Кликните на workflow → посмотрите логи ошибок

### 3.3 Проверьте сайт

Откройте ваш сайт в браузере - изменения должны быть видны!

---

## 🔧 Решение проблем

### ❌ Permission denied (publickey)

**Проблема:** GitHub Actions не может подключиться по SSH

**Решение:**
```bash
# На VPS убедитесь что ключ добавлен
cat ~/.ssh/authorized_keys | grep github-actions

# Проверьте права
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh
```

### ❌ Host key verification failed

**Проблема:** SSH не доверяет серверу

**Решение:** Добавьте в `.github/workflows/deploy.yml` после line 23:

```yaml
port: ${{ secrets.VPS_PORT }}
script_stop: false
```

### ❌ fatal: could not read Username

**Проблема:** Git на VPS запрашивает credentials

**Решение:** Используйте SSH URL для git remote:
```bash
# На VPS
cd /var/www/web-site
git remote set-url origin git@github.com:slaffka85/web-site.git

# Добавьте SSH ключ GitHub на VPS
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
cat ~/.ssh/id_rsa.pub
# Добавьте этот ключ: GitHub → Settings → SSH and GPG keys
```

### ❌ workflow всё еще не работает

1. **Перепроверьте секреты:**
   - Все 5 секретов созданы?
   - Имена написаны ТОЧНО как указано (с учетом регистра)?
   - В `VPS_SSH_KEY` скопирован весь ключ целиком?

2. **Проверьте VPS:**
   ```bash
   # Попробуйте подключиться вручную с тем же ключом
   ssh -i ~/.ssh/github_actions_key ваш_пользователь@ваш_vps_ip
   ```

3. **Посмотрите логи workflow:**
   - GitHub → Actions → кликните на failed workflow → раскройте "Deploy to VPS via SSH"
   - Скопируйте ошибку и покажите мне

---

## 🎉 Готово!

После настройки каждый `git push` в ветку `main` автоматически обновит ваш сайт на VPS!

### Как это работает:

1. Вы делаете `git push`
2. GitHub Actions запускает workflow
3. Workflow подключается к VPS по SSH
4. Выполняет `git pull` в директории проекта
5. Сайт обновлён! 🚀

---

## 📚 Дополнительная информация

- Подробное руководство: [DEPLOYMENT_GUIDE.md](file:///e:/freelance/antigravity-projects/web-site/DEPLOYMENT_GUIDE.md)
- Workflow файл: [.github/workflows/deploy.yml](file:///e:/freelance/antigravity-projects/web-site/.github/workflows/deploy.yml)
- GitHub Actions документация: https://docs.github.com/en/actions
